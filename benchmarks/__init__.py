"""Benchmark harness for semantic-chunkers.

Run with ``make bench`` (or ``uv run python -m benchmarks.run``). Writes
``benchmarks/results.json`` in the visionary results contract so the
bench-delta action can diff pull requests against main.
"""
